/** Saved form templates (from image / OCR) — same key as ERMFormFromImage. */
export const ERM_TEMPLATES_STORAGE_KEY = 'erm_dynamic_form_templates_v1';

/** Browser fallback when ERM patient-record API is not deployed yet. */
export const ERM_PATIENT_RECORDS_LOCAL_KEY = 'erm_patient_mapped_records_v1';

/** Redux-persisted patient documents (files, photos, form records) until API is ready. */
export const ERM_PATIENT_DOCUMENTS_KEY = 'erm_patient_documents_v2';
